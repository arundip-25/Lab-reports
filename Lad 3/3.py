# without exponent argument
def with_out_e(base,power):
    print(f"the number is {base**power}")
with_out_e(10,3)

#with exponent argument
import math
def with_e(base,power):
    print(f'the result is {math.pow(base,power)}')
with_e(10,4)